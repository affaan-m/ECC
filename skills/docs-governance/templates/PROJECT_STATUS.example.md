# PROJECT_STATUS.md —— 健康仪表盘（当前状态一眼看清；只写"现在怎样"，不写结构和历史）

> **本文件是项目当前健康状态的唯一真相源。** 删除区里的文件 = 已废弃，AI 和人都别再引用或重建它们。
> 顶部"红线块"每次进会话必读（危险不会自己报到）；下面的指标按需读。

## 红线块（每次进会话必读 —— 删除区 + 未决 P0/违规）

> 放这里的都是"不读就会悄悄踩雷"的东西：会被误重建的已删文件、不处理会出事的 P0。保持精简，几行为宜。

### 删除区（故意删掉，别再重建）

| 路径 | 为什么删 | 日期 | 替代物 |
|---|---|---|---|
| `src/legacy_parser.py` | 旧 v1 解析器，已被 services/parser 取代 | 2026-06-10 | `src/services/parser.py` |
| `scripts/old_sync.sh` | 同步逻辑搬进 Python | 2026-06-12 | `src/services/sync.py` |

### 未决违规 / P0 行动
- P0：`services/report.py` 880 行超红线，需拆分（跟踪于 LOG 2026-06-15）
- 覆盖率 63% 低于 80%，新加的 `cache` 模块没测试

### 审计保鲜度（腐烂速度由变更速度决定，审计频率必须跟上）
上次审计：2026-06-15 ｜ 之后 commit 数：12 ｜ 阈值：>30 个 commit 或 >30 天未审 → RED，应跑 `/governance-audit`

---

## 指标（按需读 —— 要看健康度时才读）

> 这部分只在"健康状态翻转"时更新（指标越线、违规新增/关闭），不是每个动作都改。逐动作的叙事进 `PROJECT_LOG.md`，别堆这里——堆了它就会频繁 churn、又被迫每次读。

| 指标 | 现在 | 阈值 | 状态 |
|---|---|---|---|
| 入口文件 main.py 行数 | 412 | <600 绿 / 600-800 黄 / >800 红 | 绿 |
| 测试覆盖率 | 63% | >80% 绿 / 60-80% 黄 / <60% 红 | 黄 |
| 最大单文件行数 | 880（services/report.py） | <800 | 红 |
