// Whether the "Plan my day" route optimizer is offered at all in this
// build. Off for now — it wasn't working the way it was wanted. Everything
// it's built on (RoutePlannerPage.jsx, routePlanner.js, the /plan-day
// route) is left in place as-is; this is the single switch back on if it's
// revisited later.
export const ROUTE_PLANNER_ENABLED = false;
